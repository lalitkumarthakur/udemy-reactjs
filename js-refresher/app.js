"use strict";

import { apiKey } from "./util.js";
import defaultKey from "./util.js";
// alert("Hello, world!");
console.log(`Api Key =  ${apiKey}`);
console.log(`Default Api Key = ${defaultKey}`); // default is only used when there is only 1 experit required in that file.
